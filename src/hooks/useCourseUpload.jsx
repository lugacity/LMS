import axios from "axios";
import Cookies from "js-cookie";
import { useState } from "react";
import toast from "react-hot-toast";

export const useCourseUpload = async (data) => {
  console.log(courses);

  return { setImage, setVideo, message };
};
